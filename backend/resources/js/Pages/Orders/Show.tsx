import { Link, usePage } from '@inertiajs/react';
import AppLayout from '../../Layouts/AppLayout';
type Order = { order_number: string; total: string; subtotal: string; delivery_fee: string; status: string; payment_method: string; delivery_address: { name: string; phone: string; address: string }; created_at: string; items: { id: number; quantity: number; unit_price: string; line_total: string; product?: { title: string } }[]; shop?: { name: string } };
export default function Show() { const { order } = usePage<{ order: Order }>().props; return <AppLayout><section className="mx-auto max-w-4xl px-4 py-12 sm:px-6"><Link href="/orders" className="text-sm font-bold text-teal-700">← My orders</Link><div className="mt-6 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-slate-500">{new Date(order.created_at).toLocaleString()}</p><h1 className="mt-2 text-4xl font-black">{order.order_number}</h1></div><span className="rounded-full bg-amber-100 px-4 py-2 font-bold capitalize text-amber-700">{order.status}</span></div><div className="mt-8 grid gap-5 lg:grid-cols-[1fr_18rem]"><div className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-xl font-black">Items</h2>{order.items.map((item) => <div key={item.id} className="flex justify-between gap-4 border-b border-slate-100 py-4"><div><p className="font-bold">{item.product?.title || 'Product'}</p><p className="text-sm text-slate-500">{item.quantity} × {Number(item.unit_price).toLocaleString()} MMK</p></div><p className="font-black">{Number(item.line_total).toLocaleString()} MMK</p></div>)}<div className="mt-5 flex justify-between text-xl font-black"><span>Total</span><span className="text-teal-700">{Number(order.total).toLocaleString()} MMK</span></div></div><aside className="h-fit rounded-3xl bg-slate-900 p-6 text-white"><h2 className="font-black">Delivery</h2><p className="mt-4 font-bold">{order.delivery_address.name}</p><p className="mt-1 text-sm text-slate-300">{order.delivery_address.phone}</p><p className="mt-3 text-sm leading-6 text-slate-300">{order.delivery_address.address}</p><p className="mt-6 border-t border-white/15 pt-4 text-sm capitalize text-slate-300">Payment: {order.payment_method}</p></aside></div></section></AppLayout>; }
