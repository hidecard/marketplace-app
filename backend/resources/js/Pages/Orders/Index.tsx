import { Link, usePage } from '@inertiajs/react';
import AppLayout from '../../Layouts/AppLayout';
type Order = { id: number; order_number: string; total: string; status: string; created_at: string; shop?: { name: string } };
export default function Index() { const { orders } = usePage<{ orders: { data: Order[] } }>().props; return <AppLayout><section className="mx-auto max-w-4xl px-4 py-12 sm:px-6"><p className="text-sm font-bold uppercase tracking-widest text-primary-700">Account</p><h1 className="mt-2 text-4xl font-black">My orders</h1><div className="mt-8 space-y-3">{orders.data.map((order) => <Link href={`/orders/${order.id}`} key={order.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200 hover:ring-primary-300"><div><p className="font-black">{order.order_number}</p><p className="text-sm text-slate-500">{order.shop?.name || 'Individual seller'} · {new Date(order.created_at).toLocaleDateString()}</p></div><div className="text-right"><p className="font-black text-primary-700">{Number(order.total).toLocaleString()} MMK</p><p className="text-sm capitalize text-slate-500">{order.status}</p></div></Link>)}{!orders.data.length && <p className="rounded-3xl bg-white p-12 text-center text-slate-500">No orders yet.</p>}</div></section></AppLayout>; }
