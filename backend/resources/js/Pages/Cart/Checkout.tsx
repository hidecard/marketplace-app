import { Link, useForm, usePage } from '@inertiajs/react';
import AppLayout from '../../Layouts/AppLayout';
type Item = { product: { title: string }; quantity: number; line_total: number };
export default function Checkout() { const { items, subtotal } = usePage<{ items: Item[]; subtotal: number }>().props; const form = useForm({ name: '', phone: '', address: '' }); return <AppLayout><section className="mx-auto max-w-3xl px-4 py-12 sm:px-6"><Link href="/cart" className="text-sm font-bold text-teal-700">← Back to cart</Link><h1 className="mt-6 text-4xl font-black">Checkout</h1><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_18rem]"><form onSubmit={(e) => { e.preventDefault(); form.post('/checkout'); }} className="space-y-5 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-xl font-black">Delivery details</h2><label className="block text-sm font-bold">Name<input value={form.data.name} onChange={(e) => form.setData('name', e.target.value)} className="mt-2 w-full rounded-xl border-slate-300" /></label><label className="block text-sm font-bold">Phone<input value={form.data.phone} onChange={(e) => form.setData('phone', e.target.value)} className="mt-2 w-full rounded-xl border-slate-300" /></label><label className="block text-sm font-bold">Address<textarea value={form.data.address} onChange={(e) => form.setData('address', e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border-slate-300" /></label>{Object.values(form.errors).map((error) => <p key={error} className="text-sm text-red-600">{error}</p>)}<button disabled={form.processing || !items.length} className="w-full rounded-xl bg-teal-700 px-4 py-3 font-bold text-white disabled:bg-slate-300">{form.processing ? 'Placing order…' : 'Place COD order'}</button></form><aside className="h-fit rounded-3xl bg-slate-900 p-6 text-white"><h2 className="font-black">Order summary</h2>{items.map((item) => <div key={item.product.title} className="mt-4 flex justify-between gap-3 text-sm text-slate-300"><span>{item.product.title} × {item.quantity}</span><span>{item.line_total.toLocaleString()}</span></div>)}<div className="mt-6 border-t border-white/15 pt-4 text-xl font-black">{subtotal.toLocaleString()} MMK</div><p className="mt-2 text-xs text-slate-400">Cash on delivery</p></aside></div></section></AppLayout>; }
