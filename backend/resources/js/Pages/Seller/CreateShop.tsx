import { useForm } from '@inertiajs/react';
import AppLayout from '../../Layouts/AppLayout';

export default function CreateShop() {
    const form = useForm({ name: '', description: '', phone: '', address: '' });
    return <AppLayout><section className="mx-auto max-w-2xl px-4 py-12 sm:px-6"><p className="text-sm font-bold uppercase tracking-widest text-primary-700">Seller onboarding</p><h1 className="mt-2 text-4xl font-black">Create your shop</h1><p className="mt-3 text-slate-600">Your shop starts as pending. Submit verification documents after creation before seller tools unlock.</p><form onSubmit={(e) => { e.preventDefault(); form.post('/seller/shop'); }} className="mt-8 space-y-5 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><label className="block text-sm font-bold">Shop name<input value={form.data.name} onChange={(e) => form.setData('name', e.target.value)} className="mt-2 w-full rounded-xl border-slate-300" /></label><label className="block text-sm font-bold">Description<textarea value={form.data.description} onChange={(e) => form.setData('description', e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border-slate-300" /></label><div className="grid gap-5 sm:grid-cols-2"><label className="block text-sm font-bold">Phone<input value={form.data.phone} onChange={(e) => form.setData('phone', e.target.value)} className="mt-2 w-full rounded-xl border-slate-300" /></label><label className="block text-sm font-bold">Address<input value={form.data.address} onChange={(e) => form.setData('address', e.target.value)} className="mt-2 w-full rounded-xl border-slate-300" /></label></div>{Object.values(form.errors).map((error) => <p key={error} className="text-sm text-red-600">{error}</p>)}<button disabled={form.processing} className="w-full rounded-xl bg-primary-700 px-4 py-3 font-bold text-white disabled:opacity-60">{form.processing ? 'Creating…' : 'Create shop'}</button></form></section></AppLayout>;
}
